<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dddumpling.game"
    android:versionCode="25"
    android:versionName="0.1.24">

    <uses-feature android:name="android.hardware.touchscreen" android:required="true" />

    <!--
        Without this the story narration is silent on Android 11 and up: package visibility hides
        every installed speech engine from an app targeting 30+, so TextToSpeech comes back with
        an init error and nothing else to say about it.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="true"
        android:appCategory="game"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/dumpling_launcher"
        android:label="@string/app_name">

        <activity
            android:name="com.dddumpling.game.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
